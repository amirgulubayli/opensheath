# HTTP runtime
PORT=3000
NODE_ENV=development
APP_ENV=local

# Feature toggles
ENABLE_AI_FEATURES=false
# Keep billing disabled for hackathon demo MVP (non-monetized scope).
ENABLE_BILLING=false

# Database
DATABASE_URL=postgres://ethoxford:ethoxford@localhost:5432/ethoxford

# Web
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Secrets (required only when corresponding feature toggle is true)
OPENAI_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
