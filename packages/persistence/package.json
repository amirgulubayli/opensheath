{
  "name": "@ethoxford/persistence",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "migrations"
  ],
  "dependencies": {
    "pg": "^8.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "node --loader tsx src/migrate.ts",
    "test": "echo persistence tests pending",
    "lint": "echo persistence lint handled at root"
  }
}
